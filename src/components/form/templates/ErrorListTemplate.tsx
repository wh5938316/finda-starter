import type { ErrorListProps } from '@rjsf/utils';

 
const ErrorListTemplate = (_props: ErrorListProps) => {
  // 返回 null 来隐藏顶部错误列表
  return null;
};

// noinspection JSUnusedGlobalSymbols
export default ErrorListTemplate;
